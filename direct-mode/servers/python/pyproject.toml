[project]
name = "direct-mode-token-server"
version = "0.1.0"
description = "Session token service for Spatius Direct Mode"
requires-python = ">=3.10,<3.15"
dependencies = [
  "flask>=3.0.0",
  "flask-cors>=4.0.0",
  "httpx>=0.28.1",
  "python-dotenv>=1.0.0",
]
