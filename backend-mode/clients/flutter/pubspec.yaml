name: avatar_backend_demo
description: Backend Mode Flutter client demo for AvatarKit SDK.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  spatius: ^1.0.0
  web_socket_channel: ^3.0.1
  record: ^6.2.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/api-key-guide.png
